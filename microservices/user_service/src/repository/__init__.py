"""Data repositories implement persistence layer of application.

Each data entity should have its own repository that is responsible for
persisting the data. Repositories contain the implementation for picked
database engine while providing abstract way of interaction to the
user.

Example:
"""
